import GamePage from "@/components/GamePage";

export const metadata = {
  title: "FootMon — Play",
  description: "Build your World Cup squad, compete in tournaments, or challenge opponents in 1v1 staked duels.",
};

export default function PlayRoute() {
  return <GamePage />;
}
