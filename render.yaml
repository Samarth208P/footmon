services:
  - type: web
    name: footmon
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: HOSTNAME
        value: "0.0.0.0"
      - key: NODE_VERSION
        value: 20.11.0
